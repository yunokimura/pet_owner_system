# Plan: Rename house_no to blk_lot_ph in pet_owners table

## Goal
Rename column `house_no` to `blk_lot_ph` in the `pet_owners` table and update all related code.

## Steps

### 1. Update Original Migration File
- Modify `database/migrations/2024_01_01_000002_create_pet_owners_table.php` line 23
- Change `$table->string('house_no');` to `$table->string('blk_lot_ph');`

### 2. Update PetOwner Model
- File: `app/Models/PetOwner.php` line 22
- Change `'house_no'` to `'blk_lot_ph'` in the fillable array

### 3. Update RegisteredUserController
- File: `app/Http/Controllers/Auth/RegisteredUserController.php`
- Line 91: Change `'house_no'` to `'blk_lot_ph'` in the PetOwner create array

### 4. Update ProfileController
- File: `app/Http/Controllers/ProfileController.php`
- Line 72: Change validation key from `'house_no'` to `'blk_lot_ph'`
- Line 80: Change assignment from `house_no` to `blk_lot_ph`

### 5. Update View Files
- `resources/views/profile/edit.blade.php`
  - Line 250: Change `$user->petOwner->house_no` to `$user->petOwner->blk_lot_ph`
  - Line 282: Change `name="house_no"` to `name="blk_lot_ph"`

- `resources/views/adoption_form.blade.php` line 260
- `resources/views/kapon_form.blade.php` line 268
- `resources/views/vaccination_form.blade.php` line 212
  - For each: Change `name="house_no"` to `name="blk_lot_ph"` and update the old() helper

### 6. Run Migration
- Execute `php artisan migrate` to create the table with the new column name