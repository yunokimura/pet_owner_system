# Plan: Add Inline Prefix Labels to Address Fields in Register Form

## Objective
Add visual inline prefix labels ("Blk / Lot / Ph /") to the address field in the registration form to guide users on the expected format. Keep field as a single input with no backend changes.

## Current State
- `block_lot_phase_house_no` - single text field (line 214-225)
- `street_name` - single text field (line 228-238)  
- `subdivision` - single text field (line 241-251)

## Implementation
Add small, muted format hint labels above the main address field:

```html
<!-- Block/Lot/Phase/House No. section -->
<div class="md:col-span-2">
    <label class="block text-xs font-medium text-gray-500 mb-1">
        Blk / Lot / Ph <span class="text-gray-400">(House No. optional)</span>
    </label>
    <input type="text" name="block_lot_phase_house_no" ...>
</div>
```

## Changes in `resources/views/auth/register.blade.php`
1. Around lines 214-225 - Add inline prefix label "Blk / Lot / Ph" with note that House No. is optional
2. No changes to `street_name` and `subdivision` fields - they already have clear labels
3. No changes to form functionality, validation, or database

## Notes
- Purely visual UI change - users still type in single field
- House No. is indicated as optional in the label hint
- No splitting of the current single input field