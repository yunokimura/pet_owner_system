# Plan: Contact CVO Button - Open Gmail with Pre-filled Email

## Goal
Update the "Contact CVO" button in the missing pet modal to open Gmail directly with a pre-filled email to `vetdasma@yahoo.com` including the pet name in the subject.

## Changes Needed

### File: `resources/views/welcome.blade.php`

1. **Update `contactOwner()` JavaScript function** (around line 957):
   - Change from showing an alert to opening Gmail URL
   - URL format: `https://mail.google.com/mail/?to=vetdasma@yahoo.com&subject=Inquiry about missing pet: [pet name]`
   - Need to encode the subject for URL (replace spaces with `+` or `%20`)

2. **Pass pet name to the function**:
   - Update the button onclick to pass the pet name
   - `onclick="contactOwner('{{ $pet->name }}')"`
   - Or use JavaScript to get the current pet's name from the modal

## Implementation Steps

1. Modify the button to pass pet name to the function
2. Update `contactOwner()` function to:
   - Accept pet name as parameter
   - Construct Gmail URL with encoded subject
   - Open in new tab/window

## Gmail URL Format
```
https://mail.google.com/mail/?to=vetdasma@yahoo.com&subject=Inquiry+about+missing+pet:+[pet+name]
```

## Note
The pet name needs to be retrieved from the modal - we already have `modalPetName` element that contains the pet's name, so we can get it dynamically when the button is clicked.